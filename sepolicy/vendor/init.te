allow init socket_device:sock_file create;
allow init sysfs:file setattr;
allow init { adsprpcd_file fsg_file }:filesystem { relabelto relabelfrom associate mount };
allow { adsprpcd_file fsg_file }self:filesystem associate;
